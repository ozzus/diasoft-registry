name: Registry CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      promote_team:
        description: Team for manual promotion
        required: true
        default: team1
        type: choice
        options:
          - team1
          - team2
      promote_env:
        description: Environment for manual promotion
        required: true
        default: prod
        type: choice
        options:
          - dev
          - prod
      promotion_mode:
        description: direct push or pull request
        required: true
        default: pr
        type: choice
        options:
          - direct
          - pr

permissions:
  contents: read

env:
  REGISTRY_API_IMAGE: ghcr.io/ozzus/diasoft-registry/registry-api
  REGISTRY_WORKER_IMAGE: ghcr.io/ozzus/diasoft-registry/registry-import-worker
  REGISTRY_OUTBOX_IMAGE: ghcr.io/ozzus/diasoft-registry/registry-outbox-publisher

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"
      - run: gradle test --no-daemon

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          docker run --rm -v "$PWD:/repo" zricethezav/gitleaks:v8.24.2 detect --source /repo --no-git --redact
      - run: |
          docker run --rm -v "$PWD:/repo" aquasec/trivy:0.59.1 fs --scanners vuln --severity CRITICAL --exit-code 1 --no-progress /repo
      - run: mkdir -p build
      - run: |
          docker run --rm -v "$PWD:/repo" -v "$PWD/build:/build" anchore/syft:v1.18.1 dir:/repo -o cyclonedx-json=/build/registry-sbom.cdx.json
      - uses: actions/upload-artifact@v4
        with:
          name: registry-sbom-${{ github.sha }}
          path: build/registry-sbom.cdx.json

  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"
      - run: gradle integrationTest --no-daemon

  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sh scripts/package-contracts.sh build/contracts "${GITHUB_SHA}"
      - uses: actions/upload-artifact@v4
        with:
          name: registry-contracts-${{ github.sha }}
          path: build/contracts/registry-contracts.tar.gz

  build:
    runs-on: ubuntu-latest
    needs:
      - validate
      - integration
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t "$REGISTRY_API_IMAGE:${GITHUB_SHA}" .
      - run: docker build -t "$REGISTRY_WORKER_IMAGE:${GITHUB_SHA}" .
      - run: docker build -t "$REGISTRY_OUTBOX_IMAGE:${GITHUB_SHA}" .

  publish:
    runs-on: ubuntu-latest
    needs:
      - validate
      - security
      - integration
      - contracts
      - build
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - run: echo "${{ secrets.GHCR_TOKEN || github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
      - run: docker build -t "$REGISTRY_API_IMAGE:${GITHUB_SHA}" .
      - run: docker build -t "$REGISTRY_WORKER_IMAGE:${GITHUB_SHA}" .
      - run: docker build -t "$REGISTRY_OUTBOX_IMAGE:${GITHUB_SHA}" .
      - run: docker push "$REGISTRY_API_IMAGE:${GITHUB_SHA}"
      - run: docker push "$REGISTRY_WORKER_IMAGE:${GITHUB_SHA}"
      - run: docker push "$REGISTRY_OUTBOX_IMAGE:${GITHUB_SHA}"

  sign:
    runs-on: ubuntu-latest
    needs: publish
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request' && secrets.COSIGN_PRIVATE_KEY != ''
    permissions:
      contents: read
      packages: read
    steps:
      - uses: sigstore/cosign-installer@v3
      - env:
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
        run: cosign sign --yes --key env://COSIGN_PRIVATE_KEY "$REGISTRY_API_IMAGE:${GITHUB_SHA}"
      - env:
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
        run: cosign sign --yes --key env://COSIGN_PRIVATE_KEY "$REGISTRY_WORKER_IMAGE:${GITHUB_SHA}"
      - env:
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
        run: cosign sign --yes --key env://COSIGN_PRIVATE_KEY "$REGISTRY_OUTBOX_IMAGE:${GITHUB_SHA}"

  promote-team1-dev:
    runs-on: ubuntu-latest
    needs: publish
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && secrets.PLATFORM_INFRA_GITHUB_TOKEN != ''
    steps:
      - uses: actions/checkout@v4
      - run: sudo curl -fsSL -o /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64
      - run: sudo chmod +x /usr/local/bin/yq
      - env:
          PLATFORM_INFRA_REPO_URL: https://github.com/ozzus/platform-infra.git
          PLATFORM_INFRA_GITHUB_REPO: ozzus/platform-infra
          PLATFORM_INFRA_GITHUB_TOKEN: ${{ secrets.PLATFORM_INFRA_GITHUB_TOKEN }}
          TARGET_TEAM: team1
          TARGET_ENV: dev
          PROMOTION_MODE: direct
        run: sh scripts/promote-platform-infra.sh

  promote-team2-dev:
    runs-on: ubuntu-latest
    needs: publish
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && secrets.PLATFORM_INFRA_GITHUB_TOKEN != ''
    steps:
      - uses: actions/checkout@v4
      - run: sudo curl -fsSL -o /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64
      - run: sudo chmod +x /usr/local/bin/yq
      - env:
          PLATFORM_INFRA_REPO_URL: https://github.com/ozzus/platform-infra.git
          PLATFORM_INFRA_GITHUB_REPO: ozzus/platform-infra
          PLATFORM_INFRA_GITHUB_TOKEN: ${{ secrets.PLATFORM_INFRA_GITHUB_TOKEN }}
          TARGET_TEAM: team2
          TARGET_ENV: dev
          PROMOTION_MODE: direct
        run: sh scripts/promote-platform-infra.sh

  promote-manual:
    runs-on: ubuntu-latest
    needs: publish
    if: github.event_name == 'workflow_dispatch' && secrets.PLATFORM_INFRA_GITHUB_TOKEN != ''
    steps:
      - uses: actions/checkout@v4
      - run: sudo curl -fsSL -o /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64
      - run: sudo chmod +x /usr/local/bin/yq
      - env:
          PLATFORM_INFRA_REPO_URL: https://github.com/ozzus/platform-infra.git
          PLATFORM_INFRA_GITHUB_REPO: ozzus/platform-infra
          PLATFORM_INFRA_GITHUB_TOKEN: ${{ secrets.PLATFORM_INFRA_GITHUB_TOKEN }}
          TARGET_TEAM: ${{ inputs.promote_team }}
          TARGET_ENV: ${{ inputs.promote_env }}
          PROMOTION_MODE: ${{ inputs.promotion_mode }}
        run: sh scripts/promote-platform-infra.sh
